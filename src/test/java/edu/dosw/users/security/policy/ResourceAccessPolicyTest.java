package edu.dosw.users.security.policy;

/**
 * Compatibility holder for the legacy `ResourceAccessPolicyTest` file name.
 *
 * <p>The executable tests now live in {@link UserAccessPolicyTest}.
 */
@SuppressWarnings({"squid:S2187", "java:S2187"}) // Sonar/IntelliJ: this legacy holder intentionally has no tests
final class ResourceAccessPolicyTest {
    private ResourceAccessPolicyTest() {
    }
}

